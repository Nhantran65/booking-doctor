import styled from '@emotion/styled';
import { Row } from 'antd';

export const BlogDetailsWrapper = styled.div`
    padding: 20px 10px;
    max-width: 1400px;
    margin: auto;
`;

export const ListPost = styled(Row)`
    display: flex;
    align-items: center;
    flex-wrap: wrap;
    row-gap: 20px;

`;
